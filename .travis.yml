osx_image: xcode9.3
language: objective-c

branches:
  only:
    - develop
    - master
script:
- set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace Example/SwedishSSN.xcworkspace -scheme SwedishSSN-Example -destination  'platform=iOS Simulator,name=iPhone 7,OS=11.3' ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
